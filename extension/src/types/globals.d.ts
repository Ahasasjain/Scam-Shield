/**
 * Build-time constants injected by Vite `define` (see vite.config.ts).
 * SCAMSHIELD_API_URL is set from the SCAMSHIELD_API_URL env var when
 * building for production — end users never configure an endpoint.
 */
declare const __SCAMSHIELD_API_URL__: string;
